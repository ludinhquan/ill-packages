{
  "name": "@ilotustech/ilotustech",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna exec npm install",
    "publish": "lerna publish --force-publish"
  },
  "devDependencies": {
    "lerna": "^5.3.0"
  }
}
